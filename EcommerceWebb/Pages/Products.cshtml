@page
@model EcommerceWebb.Pages.ProductsModel
@{
    <div>
        <h1>@Model.ProductName</h1>
        <ul>
            @foreach (var productName in Model.AllProducts)
            {
                <li>@productName</li>
            }
        </ul>
    </div>
}
